<h1>編集画面</h1>

<%= form_with model: @book do |f| %>

  <h4>題名</h4>
  <%= f.text_field :title %>
  
  <h4>感想</h4>
  <%= f.text_area :body %>
  
  <p><%= f.submit 'Update Book' %></p>
  
<% end %>

<%= link_to "Show", book_path(@book.id) %>
|
<a href= "/books">Back</a>